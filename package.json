{
  "name": "realreels",
  "version": "0.1.0",
  "private": true,
  "description": "RealReels — real estate reels + listings. Monorepo: mobile (RN), server (NestJS), dashboard (Next.js), shared types.",
  "workspaces": [
    "shared",
    "server",
    "dashboard"
  ],
  "scripts": {
    "server:dev": "npm --workspace server run start:dev",
    "dashboard:dev": "npm --workspace dashboard run dev",
    "shared:build": "npm --workspace shared run build"
  }
}
